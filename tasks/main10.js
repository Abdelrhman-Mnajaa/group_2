//idrees

/*
let n = prompt("Please enter two words, ex (word , word)");
n = n.split(",")

function sameLength(){
    if(n[0].length = n[1].length){
        console.log("true");
    }
    else{
        console.log("false");
    }
}
sameLength();
*/




//AHMED
/*
Write a function called sameLength
that accepts two strings as arguments,
and returns true if those strings have the same length, and false otherwise.

**hint: how we can know string length   Ex: : "tree".length   => 4

Ex: sameLength("tree","clue")
=> true

Ex: sameLength("tree","car")
=> false
*/
/*
function sameLength(k,p){
    if (k.length === p.length)
    {
        console.log("Ture");
    }
    else {
        console.log("False");
    }
    
}
let z= Number(prompt("Enter name"))
let x= Number(prompt("Enter name"))
console.log(sameLength(z,x))
*/