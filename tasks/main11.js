//idrees

/*
let n = prompt("Please enter two numbers, ex (number, number)");
n = n.split(",").map(Number);

function largerNubmer(){
    if( n[0] > n[1]){
        console.log(n[0] + " is bigger than " + n[1] + ".");
    }
    else if(n[0] < n[1]){
        console.log(n[1] + " is bigger than " + n[0] + ".");
    }
    else if( n[0] = n[1] ){
        console.log( n[0] + " and " + n[1] + " are equal.");
    }
}
largerNubmer();
*/